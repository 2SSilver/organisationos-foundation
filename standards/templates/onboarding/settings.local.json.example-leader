{
  "//": "Copy to .claude/settings.local.json (gitignored). Leader: add Leadership when working in a Domain repo; when working in the Leadership repo, point at Foundation + any Domain repos instead.",
  "permissions": {
    "additionalDirectories": [
      "../organisationos-foundation",
      "../organisationos-leadership"
    ]
  }
}
