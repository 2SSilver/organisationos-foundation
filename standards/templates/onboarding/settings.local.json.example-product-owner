{
  "//": "Copy to .claude/settings.local.json in your Domain repo clone (gitignored). Product Owner: Domain + Foundation.",
  "permissions": {
    "additionalDirectories": [
      "../organisationos-foundation"
    ]
  }
}
